# Environment secrets - never commit real secrets
.env

# Python
__pycache__/
*.pyc
*.db
venv/
.venv/

# Alembic (keep migrations, ignore local sqlite test dbs)
*.sqlite3

# Google service account key (Database & Sheets module).
# This grants write access to any spreadsheet it's shared with — never commit it.
secrets/
*service_account*.json

# Pytest
.pytest_cache/
