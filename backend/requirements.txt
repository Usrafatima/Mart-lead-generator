fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
alembic==1.13.3
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0
python-jose[cryptography]==3.3.0
passlib==1.7.4
bcrypt==4.0.1
python-multipart==0.0.9
redis==5.0.8
celery==5.4.0
python-dotenv==1.0.1

# Scraper dependencies. Already imported by app/bots/website_scraper/ and
# app/bots/google_maps.py but not previously pinned here, so `pip install -r
# requirements.txt` produced an app that couldn't start (ModuleNotFoundError:
# bs4 on `from app.main import app`).
beautifulsoup4==4.12.3
requests==2.32.3
lxml==5.3.0
playwright==1.47.0

# Google Sheets export (Database & Sheets module)
gspread==6.1.2
google-auth==2.35.0
pandas==2.2.3
openpyxl==3.1.5

# Tests
pytest==8.3.3
httpx==0.27.2
